/**
 * The curated palette offered before the free-form picker in every colour
 * control: theme panel fields and the editor's inline text/background
 * pickers. Ten swatches — neutrals first, then the hues an email actually
 * uses — so most people never need the RGB picker at all.
 */
export const COLOR_PRESETS = [
  '#000000',
  '#FFFFFF',
  '#6B7280',
  '#4F46E5',
  '#2563EB',
  '#0D9488',
  '#16A34A',
  '#D97706',
  '#DC2626',
  '#9333EA',
] as const;
